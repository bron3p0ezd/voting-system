# Интерфейс и порт Vite dev-сервера.
VITE_DEV_HOST=localhost
VITE_DEV_PORT=5173

# Разрешённые CORS-origin для dev-сервера через запятую.
VITE_DEV_ALLOWED_ORIGINS=http://localhost:5173

# Базовый адрес backend API без завершающего слеша.
VITE_API_BASE_URL=http://localhost:8000/api/v1
